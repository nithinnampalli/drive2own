<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:Drive2Own.Mobile.Views"
       x:Class="Drive2Own.Mobile.AppShell"
       Shell.NavBarIsVisible="True">

    <!-- Auth routes (not visible in tab bar) -->
    <ShellContent Route="login" ContentTemplate="{DataTemplate views:LoginPage}" Shell.NavBarIsVisible="False"/>
    <ShellContent Route="register" ContentTemplate="{DataTemplate views:RegisterPage}" Shell.NavBarIsVisible="False"/>

    <!-- Main tabs -->
    <TabBar Route="main">
        <ShellContent Title="Dashboard" Icon="dashboard" Route="dashboard" ContentTemplate="{DataTemplate views:DashboardPage}"/>
        <ShellContent Title="Map" Icon="map" Route="map" ContentTemplate="{DataTemplate views:MapPage}"/>
        <ShellContent Title="Track" Icon="track" Route="track" ContentTemplate="{DataTemplate views:TrackingPage}"/>
        <ShellContent Title="Routes" Icon="routes" Route="routes" ContentTemplate="{DataTemplate views:RoutesPage}"/>
    </TabBar>

</Shell>
